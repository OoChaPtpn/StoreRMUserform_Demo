import theme, { COLORS, SIZES, FONTS, lightTheme, darkTheme } from "./theme";
import ROUTES from './routes';

export {
    theme,
    COLORS,
    SIZES,
    FONTS,
    lightTheme,
    darkTheme,
    ROUTES,
}